# Optional override. Default uses current app origin and Vite proxies /v1 to localhost:45187.
# VITE_STARCITE_BASE_URL=http://localhost:45187

# Optional if your local Starcite instance requires auth.
# VITE_STARCITE_API_KEY=sk_local_dev

# Optional creator principal to satisfy session creation on deployments that
# require explicit creator identity when API key is not in-browser.
# VITE_STARCITE_CREATOR_TENANT_ID=anor-ai
# VITE_STARCITE_CREATOR_ID=org:anor-ai
# VITE_STARCITE_CREATOR_TYPE=user

# Optional dev-server proxy settings for remote testing.
# These are read by vite.config.ts from process env at startup.
# STARCITE_PROXY_TARGET=https://<your-instance>.starcite.io
# STARCITE_PROXY_API_KEY=<YOUR_API_KEY>
