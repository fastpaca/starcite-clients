{
  "name": "starcite-clients",
  "private": true,
  "version": "0.1.0",
  "description": "Starcite client monorepo",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "ultracite check .",
    "format": "ultracite fix .",
    "typecheck": "pnpm -r typecheck",
    "starcite": "pnpm --filter starcite dev"
  },
  "devDependencies": {
    "ultracite": "^7.1.5"
  }
}
